# Sidepad pad for Signal
# Defines the application command, window class, and optional controller options.

# Launch command (kwallet integration is required on this system)
SIDEPAD_APP="signal-desktop --password-store=kwallet6"

# Hyprland window class to match
SIDEPAD_CLASS="Signal"

# Optional controller options (leave empty if not needed)
SIDEPAD_OPTIONS=""
